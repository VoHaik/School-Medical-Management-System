# Development local environment configuration (highest precedence in development)
# Fix for "options.allowedHosts[0] should be a non-empty string" error

# Most direct fix for allowedHosts issue
WDS_ALLOWED_HOSTS=localhost,127.0.0.1

# Configure webpack dev server
HOST=localhost
PORT=3000

# Disable host check as a fallback
DANGEROUSLY_DISABLE_HOST_CHECK=true