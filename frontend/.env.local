# Local environment configuration (highest precedence)
# Fix for "options.allowedHosts[0] should be a non-empty string" error

# Direct fix for allowedHosts issue
REACT_APP_ALLOWED_HOSTS=localhost

# Configure webpack dev server
HOST=localhost
PORT=3000

# Fallback option if other settings don't work
DANGEROUSLY_DISABLE_HOST_CHECK=true